import React from 'react';
// import RecipeContext from '../context/RecipesContext';

function DetailFoods() {
  // const { arrayMeals } = useContext(RecipeContext);

  return (
    <h1>
      Detail Foods
    </h1>
  );
}

export default DetailFoods;
